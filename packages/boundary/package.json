{
  "name": "@intentive/boundary",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared parse-at-boundary decode: one leak-free parseBoundary/BoundaryParseError for every inbound boundary (WebSocket + HTTP). See docs/adr/0004.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
