{
  "name": "@intentive/api-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Control Plane HTTP request/response schemas. Public client API plus internal CP↔Agent Runtime API.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test test/**/*.test.mjs"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "@intentive/domain-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
