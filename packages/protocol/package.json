{
  "name": "@intentive/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WebSocket message contract every client speaks and the Agent Runtime understands. Single source of truth for the wire format.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "@intentive/boundary": "workspace:*",
    "@intentive/domain-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
