# @intentive/domain-types

Shared domain shapes that are not tied to a wire format (which lives in `@intentive/protocol` and `@intentive/api-contract`).

Use when a concept needs the same TypeScript type in multiple deployables but isn't sent over the network as-is.
