# Reference screenshots

The source screenshots the Apex Design System was derived from. These were provided directly by the
user (the upstream copies exceed the design MCP's 256 KiB per-file read cap and can't be pulled
through it), converted from webp to PNG:

- `mobile-screens.png` — three iPhone mocks: Dashboard, Progress, AI Chat
- `desktop-dashboard-flat.png` — desktop dashboard (sidebar + content + AI chat panel)
- `desktop-dashboard-framed.png` — a copy of the same desktop shot; a distinct "framed" render was
  not provided separately, so this duplicate exists only so `readme.md`'s reference resolves.

`../../guidelines/reference-screens.card.html` displays `mobile-screens.png` and
`desktop-dashboard-flat.png`.
