Generic rounded surface — the base every stat card, panel, and chat container sits on.

```jsx
<Card variant="muted" padding="24px">…</Card>
<Card variant="dark" radius="var(--radius-l)">…</Card>
```
