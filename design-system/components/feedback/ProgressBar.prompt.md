Linear goal-progress track — a pill-shaped groove with a white fill pill, set inside a muted card (the "45% to your daily goal" bar).

```jsx
<ProgressBar value={2340} max={8000} valueLabel="2,340/8000" />
```
