Pill-shaped tab switcher with a floating white "thumb" behind the active label — used for Daily/Weekly/Monthly and similar 2–4 option toggles.

```jsx
<SegmentedControl options={['Daily', 'Weekly', 'Monthly']} defaultValue="Weekly" />
```
