{
  "name": "Desktop Client",
  "scope": "apps/desktop",
  "aliases": ["desktop"],
  "owningContextDocs": [
    "CONTEXT-MAP.md",
    "apps/desktop/CONTEXT.md",
    "apps/desktop/ARCHITECTURE.md",
    "apps/desktop/docs/SMOKE.md",
    "ARCHITECTURE.md",
    "docs/TESTING.md"
  ],
  "adrDirs": ["docs/adr", "apps/desktop/docs/adr"],
  "requiredCommands": [
    {
      "label": "Desktop typecheck",
      "command": "pnpm",
      "args": ["--dir", "apps/desktop", "typecheck"]
    },
    {
      "label": "Desktop Swift tests",
      "command": "pnpm",
      "args": ["--dir", "apps/desktop", "test"]
    },
    {
      "label": "Desktop assembled acceptance",
      "command": "pnpm",
      "args": ["--dir", "apps/desktop", "acceptance:assembled"]
    },
    {
      "label": "Desktop release bundle smoke",
      "command": "pnpm",
      "args": ["--dir", "apps/desktop", "release:smoke"]
    }
  ],
  "sensors": [
    {
      "label": "Impact radius sensor",
      "command": "pnpm",
      "args": ["sensor:impact-radius"]
    }
  ],
  "highRiskSharedPackages": [
    "packages/protocol",
    "packages/api-contract",
    "packages/domain-types",
    "packages/providers"
  ],
  "commonFailureModes": [
    "Desktop must not publish raw frames or audio; only compact perception_event records cross the Runtime Bridge.",
    "The text-only Floating Bar must join the one Companion conversation instead of creating a second desktop chat store.",
    "Swift Codable protocol fixtures must stay aligned with packages/protocol/test/fixtures.",
    "Desktop CI and release jobs are path-sensitive; shared package changes can still affect Desktop assumptions."
  ],
  "reviewHints": [
    "Provider changes should inspect desktop auth and telemetry seams.",
    "Protocol changes should inspect Runtime Bridge Codable fixtures, perception publishing, and delivery acknowledgements."
  ]
}
