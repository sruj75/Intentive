# Factory Decisions

Use this folder for ADR-style records when the factory makes an important trade-off.

Examples:

- why a recurring vocabulary hit was accepted
- why a sensor stays advisory instead of blocking CI
- why a boundary rule exception was allowed

Each decision should link back to ledger IDs and any related backlog item.
