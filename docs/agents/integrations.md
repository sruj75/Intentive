# ScreenPipe and Ollama

Read when integrating, debugging, or changing capture/LLM boundaries.

## Decisions

Read `docs/adr/` for ports, process ownership, and integration seams before changing behavior.

## Operational skills

Use `.claude/skills/screenpipe-*` for health checks, REST API queries, CLI pipes/connections, and log analysis when debugging capture.
