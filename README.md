# v1-deepagent

OpenClaw-like **agent shell** on **LangChain DeepAgents (TypeScript)**: gateway, sessions, channels, cron, heartbeat, workspace loading. DeepAgents is the brain.

## Reference for coding agents

Upstream patterns live under [`reference/`](reference/). Read [`reference/AGENTS.md`](reference/AGENTS.md) first, then the topic card for your task.

Regenerate Repomix packs and section anchors:

```bash
node scripts/generate-reference-llms.mjs
```
