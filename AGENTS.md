# v1-deepagent

OpenClaw-like **agent shell** (gateway, sessions, cron, channels, …) on **LangChain DeepAgents (TypeScript)**. DeepAgents is the brain; we build the always-on control plane.

For upstream pattern reference (OpenClaw + Hermes), read **[reference/AGENTS.md](reference/AGENTS.md)** — start at a topic card, not the raw `*-llms.txt` packs.
