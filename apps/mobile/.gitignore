.DS_Store

# Build / test output
dist/

# EAS local build artifacts
build-*.tar.gz

# Expo
.expo/
web-build/

# Native projects are generated by `expo prebuild` (CNG) — not committed.
# Regenerate with `npx expo prebuild -p ios`. See docs/adr/0017.
/ios
/android
