/** Companion Chat route — navigation-only shell; composition lives in ChatEntry. */
import { ChatEntry } from "../../src/entrypoints/chat-entry";

export default function ChatRoute(): React.JSX.Element {
  return <ChatEntry />;
}
