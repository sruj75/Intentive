/** Free Trial route — thin shell; renders the `onboarding` domain's screen. */
import { FreeTrial } from "../../src/domains/onboarding/ui/free-trial";

export default function TrialRoute(): React.JSX.Element {
  return <FreeTrial />;
}
