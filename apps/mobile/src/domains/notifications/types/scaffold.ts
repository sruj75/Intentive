/**
 * notifications domain scaffold (Mobile Client). Placeholder so the layered
 * domain tree is tracked by git and covered by typecheck + architecture lint.
 * Replace with real `types` and add sibling layers as the domain is implemented.
 */
export const mobileNotificationsDomainScaffold = true;
