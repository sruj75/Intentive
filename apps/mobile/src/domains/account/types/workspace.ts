/**
 * Account domain — app info / workspace readiness. Kept intentionally minimal
 * so Turbo includes the mobile deployable in typecheck/lint workflows while
 * implementation domains are migrated in.
 */
export const MOBILE_WORKSPACE_READY = true;
