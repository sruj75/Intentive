{
  "name": "@intentive/mobile",
  "version": "0.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/write-esm-marker.mjs",
    "test": "pnpm --filter \"@intentive/mobile...\" run build && node --test",
    "//test:rn": "--forceExit: @assistant-ui/tap's scheduler opens a module-scoped MessageChannel singleton at import time (a ref'd MESSAGEPORT handle with no teardown API). It can't be cleaned up — the port stays ref'd by design via its onmessage scheduler listener — so without this flag Jest hangs after the suite passes ('Jest did not exit...'), a CI footgun. Verified via --detectOpenHandles. See jest.config.js.",
    "test:rn": "jest --forceExit",
    "typecheck": "tsc --noEmit",
    "dev": "expo start",
    "ios": "expo run:ios",
    "android": "expo run:android"
  },
  "dependencies": {
    "@assistant-ui/core": "0.2.9",
    "@assistant-ui/react-native": "^0.1.20",
    "@better-auth/expo": "1.4.18",
    "@expo/metro-runtime": "~56.0.13",
    "@intentive/api-contract": "workspace:*",
    "@intentive/domain-types": "workspace:*",
    "@intentive/protocol": "workspace:*",
    "better-auth": "1.4.18",
    "expo": "^56.0.8",
    "expo-constants": "*",
    "expo-linking": "*",
    "expo-network": "~56.0.4",
    "expo-router": "*",
    "expo-secure-store": "~56.0.4",
    "expo-status-bar": "*",
    "expo-web-browser": "~56.0.5",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-native": "*",
    "react-native-safe-area-context": "^5.7.0",
    "react-native-screens": "*",
    "react-native-web": "^0.21.2"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.29.7",
    "@testing-library/react-native": "*",
    "@types/react": "*",
    "babel-preset-expo": "~56.0.14",
    "jest": "^29.7.0",
    "jest-expo": "*",
    "react-test-renderer": "^19.2.3",
    "typescript": "^6.0.3"
  }
}
