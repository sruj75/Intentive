# ADR 0006: Sparkle appcasts are release-hosted

## Status

Accepted.

## Decision

Desktop updates use Sparkle with signed appcasts hosted alongside release artifacts. The initial target is GitHub Releases plus a static signed `appcast.xml`; alternate hosting can replace it later without changing the app update boundary.
