/**
 * cron domain scaffold (Agent Runtime). Placeholder so the layered domain tree
 * is tracked by git and covered by typecheck + architecture lint. Replace with
 * real `types` and add sibling layers as the domain is implemented.
 */
export const agentRuntimeCronDomainScaffold = true;
