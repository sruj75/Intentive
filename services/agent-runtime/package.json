{
  "name": "@intentive/agent-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --outDir dist",
    "test": "pnpm --filter @intentive/protocol build && pnpm run build && node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@intentive/api-contract": "workspace:*",
    "@intentive/protocol": "workspace:*",
    "@intentive/providers": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
