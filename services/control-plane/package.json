{
  "name": "@intentive/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --outDir dist",
    "migrate": "node scripts/apply-migrations.mjs",
    "start": "node dist/main.js",
    "test": "pnpm --filter \"@intentive/control-plane...\" run build && node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.4",
    "@intentive/api-contract": "workspace:*",
    "@intentive/providers": "workspace:*",
    "@neondatabase/serverless": "^1.1.0",
    "expo-server-sdk": "^6.1.0",
    "hono": "^4.12.25",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
